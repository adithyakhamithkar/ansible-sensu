{
  "checks": {
    "nginx_check": {
      "handlers": ["email"],
      "command": "/opt/sensu/embedded/bin/ruby /etc/sensu/plugins/check-procs.rb -p nginx -C 1",
      "interval": 60,
      "subscribers": [
        "nginx",
      ]
    }
  }
